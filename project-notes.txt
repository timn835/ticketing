-Starting a typescript Node project:
	npm init -y
	npm install typescript ts-node-dev express @types/express #installs dependencies
	tsc --init #creates typescript config file

What to do for production grade application
-Set expiration time back to 15min instead of 1min for orders/new
-Implement RabbitMQ instead of NATS streaming server
-Implement Events Collection + Mongo Transactions for tickets and potentially others, see lecture 341, cutting corner
-For client's public stripe key, create another k8s secret as an evironment variable, look at Next's docs to see how to use environment variables with Next 
-